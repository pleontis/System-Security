Leontis Panagiotis AM:2018030099

$ gcc --version
gcc (Ubuntu 9.3.0-17ubuntu1~20.04)

make clean
make all
./assign_3 -g  --For generating public and private keys 
./assign_3 -i <input_file> -o <output_file> -k <key_file> -e for encryption or -d for decryption

Στο αρχείο utils.c προστέθηκαν οι συναρτήσεις για την αποθήκευση των 2 μερών ενός κλειδιού στο αντίστοιχο αρχείο. Επιπλέον υλοποιήθηκε αντίστοιχη συνάρτηση η οποία διαβάζει το κλειδί από το αρχείο το οποίο δίνεται ως όρισμα και το χωρίζει στα 2 του μέρη. Έπειτα η readFile() όπου διαβάζει το αρχείο για να πάρουμε το plaintext και επιστρέφει το πόσους χαρακτήρες διάβασε.

Το πρόγραμμα δουλεύει κανονικά. Συμβουλέυτηκα το wikipedia για την υλοποίηση του αλγορίθμου και πρόσεξα τα σημεία όπου η ασκήση ζητούσε διαφοερικά πράγματα από αυτά που έλεγε στο wiki. 
Υπάρχουν σχόλια στον κώδικα όπου εξηγείται επαρκώς το κάθε βήμα.


